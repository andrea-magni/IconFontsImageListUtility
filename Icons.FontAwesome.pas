unit Icons.FontAwesome;

interface

type
  TFontAwesome = record // 1438 entries
  public
    const _500px: Integer = $f26e; // 500px
    const accessible_icon: Integer = $f368; // accessible-icon
    const accusoft: Integer = $f369; // accusoft
    const acquisitions_incorporated: Integer = $f6af; // acquisitions-incorporated
    const ad: Integer = $f641; // ad
    const address_book: Integer = $f2b9; // address-book
    const address_card: Integer = $f2bb; // address-card
    const adjust: Integer = $f042; // adjust
    const adn: Integer = $f170; // adn
    const adobe: Integer = $f778; // adobe
    const adversal: Integer = $f36a; // adversal
    const affiliatetheme: Integer = $f36b; // affiliatetheme
    const air_freshener: Integer = $f5d0; // air-freshener
    const airbnb: Integer = $f834; // airbnb
    const algolia: Integer = $f36c; // algolia
    const align_center: Integer = $f037; // align-center
    const align_justify: Integer = $f039; // align-justify
    const align_left: Integer = $f036; // align-left
    const align_right: Integer = $f038; // align-right
    const alipay: Integer = $f642; // alipay
    const allergies: Integer = $f461; // allergies
    const amazon: Integer = $f270; // amazon
    const amazon_pay: Integer = $f42c; // amazon-pay
    const ambulance: Integer = $f0f9; // ambulance
    const american_sign_language_interpreting: Integer = $f2a3; // american-sign-language-interpreting
    const amilia: Integer = $f36d; // amilia
    const anchor: Integer = $f13d; // anchor
    const android: Integer = $f17b; // android
    const angellist: Integer = $f209; // angellist
    const angle_double_down: Integer = $f103; // angle-double-down
    const angle_double_left: Integer = $f100; // angle-double-left
    const angle_double_right: Integer = $f101; // angle-double-right
    const angle_double_up: Integer = $f102; // angle-double-up
    const angle_down: Integer = $f107; // angle-down
    const angle_left: Integer = $f104; // angle-left
    const angle_right: Integer = $f105; // angle-right
    const angle_up: Integer = $f106; // angle-up
    const angry: Integer = $f556; // angry
    const angrycreative: Integer = $f36e; // angrycreative
    const angular: Integer = $f420; // angular
    const ankh: Integer = $f644; // ankh
    const app_store: Integer = $f36f; // app-store
    const app_store_ios: Integer = $f370; // app-store-ios
    const apper: Integer = $f371; // apper
    const apple: Integer = $f179; // apple
    const apple_alt: Integer = $f5d1; // apple-alt
    const apple_pay: Integer = $f415; // apple-pay
    const archive: Integer = $f187; // archive
    const archway: Integer = $f557; // archway
    const arrow_alt_circle_down: Integer = $f358; // arrow-alt-circle-down
    const arrow_alt_circle_left: Integer = $f359; // arrow-alt-circle-left
    const arrow_alt_circle_right: Integer = $f35a; // arrow-alt-circle-right
    const arrow_alt_circle_up: Integer = $f35b; // arrow-alt-circle-up
    const arrow_circle_down: Integer = $f0ab; // arrow-circle-down
    const arrow_circle_left: Integer = $f0a8; // arrow-circle-left
    const arrow_circle_right: Integer = $f0a9; // arrow-circle-right
    const arrow_circle_up: Integer = $f0aa; // arrow-circle-up
    const arrow_down: Integer = $f063; // arrow-down
    const arrow_left: Integer = $f060; // arrow-left
    const arrow_right: Integer = $f061; // arrow-right
    const arrow_up: Integer = $f062; // arrow-up
    const arrows_alt: Integer = $f0b2; // arrows-alt
    const arrows_alt_h: Integer = $f337; // arrows-alt-h
    const arrows_alt_v: Integer = $f338; // arrows-alt-v
    const artstation: Integer = $f77a; // artstation
    const assistive_listening_systems: Integer = $f2a2; // assistive-listening-systems
    const asterisk: Integer = $f069; // asterisk
    const asymmetrik: Integer = $f372; // asymmetrik
    const at: Integer = $f1fa; // at
    const atlas: Integer = $f558; // atlas
    const atlassian: Integer = $f77b; // atlassian
    const atom: Integer = $f5d2; // atom
    const audible: Integer = $f373; // audible
    const audio_description: Integer = $f29e; // audio-description
    const autoprefixer: Integer = $f41c; // autoprefixer
    const avianex: Integer = $f374; // avianex
    const aviato: Integer = $f421; // aviato
    const award: Integer = $f559; // award
    const aws: Integer = $f375; // aws
    const baby: Integer = $f77c; // baby
    const baby_carriage: Integer = $f77d; // baby-carriage
    const backspace: Integer = $f55a; // backspace
    const backward: Integer = $f04a; // backward
    const bacon: Integer = $f7e5; // bacon
    const bahai: Integer = $f666; // bahai
    const balance_scale: Integer = $f24e; // balance-scale
    const balance_scale_left: Integer = $f515; // balance-scale-left
    const balance_scale_right: Integer = $f516; // balance-scale-right
    const ban: Integer = $f05e; // ban
    const band_aid: Integer = $f462; // band-aid
    const bandcamp: Integer = $f2d5; // bandcamp
    const barcode: Integer = $f02a; // barcode
    const bars: Integer = $f0c9; // bars
    const baseball_ball: Integer = $f433; // baseball-ball
    const basketball_ball: Integer = $f434; // basketball-ball
    const bath: Integer = $f2cd; // bath
    const battery_empty: Integer = $f244; // battery-empty
    const battery_full: Integer = $f240; // battery-full
    const battery_half: Integer = $f242; // battery-half
    const battery_quarter: Integer = $f243; // battery-quarter
    const battery_three_quarters: Integer = $f241; // battery-three-quarters
    const battle_net: Integer = $f835; // battle-net
    const bed: Integer = $f236; // bed
    const beer: Integer = $f0fc; // beer
    const behance: Integer = $f1b4; // behance
    const behance_square: Integer = $f1b5; // behance-square
    const bell: Integer = $f0f3; // bell
    const bell_slash: Integer = $f1f6; // bell-slash
    const bezier_curve: Integer = $f55b; // bezier-curve
    const bible: Integer = $f647; // bible
    const bicycle: Integer = $f206; // bicycle
    const biking: Integer = $f84a; // biking
    const bimobject: Integer = $f378; // bimobject
    const binoculars: Integer = $f1e5; // binoculars
    const biohazard: Integer = $f780; // biohazard
    const birthday_cake: Integer = $f1fd; // birthday-cake
    const bitbucket: Integer = $f171; // bitbucket
    const bitcoin: Integer = $f379; // bitcoin
    const bity: Integer = $f37a; // bity
    const black_tie: Integer = $f27e; // black-tie
    const blackberry: Integer = $f37b; // blackberry
    const blender: Integer = $f517; // blender
    const blender_phone: Integer = $f6b6; // blender-phone
    const blind: Integer = $f29d; // blind
    const blog: Integer = $f781; // blog
    const blogger: Integer = $f37c; // blogger
    const blogger_b: Integer = $f37d; // blogger-b
    const bluetooth: Integer = $f293; // bluetooth
    const bluetooth_b: Integer = $f294; // bluetooth-b
    const bold: Integer = $f032; // bold
    const bolt: Integer = $f0e7; // bolt
    const bomb: Integer = $f1e2; // bomb
    const bone: Integer = $f5d7; // bone
    const bong: Integer = $f55c; // bong
    const book: Integer = $f02d; // book
    const book_dead: Integer = $f6b7; // book-dead
    const book_medical: Integer = $f7e6; // book-medical
    const book_open: Integer = $f518; // book-open
    const book_reader: Integer = $f5da; // book-reader
    const bookmark: Integer = $f02e; // bookmark
    const bootstrap: Integer = $f836; // bootstrap
    const border_all: Integer = $f84c; // border-all
    const border_none: Integer = $f850; // border-none
    const border_style: Integer = $f853; // border-style
    const bowling_ball: Integer = $f436; // bowling-ball
    const box: Integer = $f466; // box
    const box_open: Integer = $f49e; // box-open
    const box_tissue: Integer = $f95b; // box-tissue
    const boxes: Integer = $f468; // boxes
    const braille: Integer = $f2a1; // braille
    const brain: Integer = $f5dc; // brain
    const bread_slice: Integer = $f7ec; // bread-slice
    const briefcase: Integer = $f0b1; // briefcase
    const briefcase_medical: Integer = $f469; // briefcase-medical
    const broadcast_tower: Integer = $f519; // broadcast-tower
    const broom: Integer = $f51a; // broom
    const brush: Integer = $f55d; // brush
    const btc: Integer = $f15a; // btc
    const buffer: Integer = $f837; // buffer
    const bug: Integer = $f188; // bug
    const building: Integer = $f1ad; // building
    const bullhorn: Integer = $f0a1; // bullhorn
    const bullseye: Integer = $f140; // bullseye
    const burn: Integer = $f46a; // burn
    const buromobelexperte: Integer = $f37f; // buromobelexperte
    const bus: Integer = $f207; // bus
    const bus_alt: Integer = $f55e; // bus-alt
    const business_time: Integer = $f64a; // business-time
    const buy_n_large: Integer = $f8a6; // buy-n-large
    const buysellads: Integer = $f20d; // buysellads
    const calculator: Integer = $f1ec; // calculator
    const calendar: Integer = $f133; // calendar
    const calendar_alt: Integer = $f073; // calendar-alt
    const calendar_check: Integer = $f274; // calendar-check
    const calendar_day: Integer = $f783; // calendar-day
    const calendar_minus: Integer = $f272; // calendar-minus
    const calendar_plus: Integer = $f271; // calendar-plus
    const calendar_times: Integer = $f273; // calendar-times
    const calendar_week: Integer = $f784; // calendar-week
    const camera: Integer = $f030; // camera
    const camera_retro: Integer = $f083; // camera-retro
    const campground: Integer = $f6bb; // campground
    const canadian_maple_leaf: Integer = $f785; // canadian-maple-leaf
    const candy_cane: Integer = $f786; // candy-cane
    const cannabis: Integer = $f55f; // cannabis
    const capsules: Integer = $f46b; // capsules
    const car: Integer = $f1b9; // car
    const car_alt: Integer = $f5de; // car-alt
    const car_battery: Integer = $f5df; // car-battery
    const car_crash: Integer = $f5e1; // car-crash
    const car_side: Integer = $f5e4; // car-side
    const caravan: Integer = $f8ff; // caravan
    const caret_down: Integer = $f0d7; // caret-down
    const caret_left: Integer = $f0d9; // caret-left
    const caret_right: Integer = $f0da; // caret-right
    const caret_square_down: Integer = $f150; // caret-square-down
    const caret_square_left: Integer = $f191; // caret-square-left
    const caret_square_right: Integer = $f152; // caret-square-right
    const caret_square_up: Integer = $f151; // caret-square-up
    const caret_up: Integer = $f0d8; // caret-up
    const carrot: Integer = $f787; // carrot
    const cart_arrow_down: Integer = $f218; // cart-arrow-down
    const cart_plus: Integer = $f217; // cart-plus
    const cash_register: Integer = $f788; // cash-register
    const cat: Integer = $f6be; // cat
    const cc_amazon_pay: Integer = $f42d; // cc-amazon-pay
    const cc_amex: Integer = $f1f3; // cc-amex
    const cc_apple_pay: Integer = $f416; // cc-apple-pay
    const cc_diners_club: Integer = $f24c; // cc-diners-club
    const cc_discover: Integer = $f1f2; // cc-discover
    const cc_jcb: Integer = $f24b; // cc-jcb
    const cc_mastercard: Integer = $f1f1; // cc-mastercard
    const cc_paypal: Integer = $f1f4; // cc-paypal
    const cc_stripe: Integer = $f1f5; // cc-stripe
    const cc_visa: Integer = $f1f0; // cc-visa
    const centercode: Integer = $f380; // centercode
    const centos: Integer = $f789; // centos
    const certificate: Integer = $f0a3; // certificate
    const chair: Integer = $f6c0; // chair
    const chalkboard: Integer = $f51b; // chalkboard
    const chalkboard_teacher: Integer = $f51c; // chalkboard-teacher
    const charging_station: Integer = $f5e7; // charging-station
    const chart_area: Integer = $f1fe; // chart-area
    const chart_bar: Integer = $f080; // chart-bar
    const chart_line: Integer = $f201; // chart-line
    const chart_pie: Integer = $f200; // chart-pie
    const check: Integer = $f00c; // check
    const check_circle: Integer = $f058; // check-circle
    const check_double: Integer = $f560; // check-double
    const check_square: Integer = $f14a; // check-square
    const cheese: Integer = $f7ef; // cheese
    const chess: Integer = $f439; // chess
    const chess_bishop: Integer = $f43a; // chess-bishop
    const chess_board: Integer = $f43c; // chess-board
    const chess_king: Integer = $f43f; // chess-king
    const chess_knight: Integer = $f441; // chess-knight
    const chess_pawn: Integer = $f443; // chess-pawn
    const chess_queen: Integer = $f445; // chess-queen
    const chess_rook: Integer = $f447; // chess-rook
    const chevron_circle_down: Integer = $f13a; // chevron-circle-down
    const chevron_circle_left: Integer = $f137; // chevron-circle-left
    const chevron_circle_right: Integer = $f138; // chevron-circle-right
    const chevron_circle_up: Integer = $f139; // chevron-circle-up
    const chevron_down: Integer = $f078; // chevron-down
    const chevron_left: Integer = $f053; // chevron-left
    const chevron_right: Integer = $f054; // chevron-right
    const chevron_up: Integer = $f077; // chevron-up
    const child: Integer = $f1ae; // child
    const chrome: Integer = $f268; // chrome
    const chromecast: Integer = $f838; // chromecast
    const church: Integer = $f51d; // church
    const circle: Integer = $f111; // circle
    const circle_notch: Integer = $f1ce; // circle-notch
    const city: Integer = $f64f; // city
    const clinic_medical: Integer = $f7f2; // clinic-medical
    const clipboard: Integer = $f328; // clipboard
    const clipboard_check: Integer = $f46c; // clipboard-check
    const clipboard_list: Integer = $f46d; // clipboard-list
    const clock: Integer = $f017; // clock
    const clone: Integer = $f24d; // clone
    const closed_captioning: Integer = $f20a; // closed-captioning
    const cloud: Integer = $f0c2; // cloud
    const cloud_download_alt: Integer = $f381; // cloud-download-alt
    const cloud_meatball: Integer = $f73b; // cloud-meatball
    const cloud_moon: Integer = $f6c3; // cloud-moon
    const cloud_moon_rain: Integer = $f73c; // cloud-moon-rain
    const cloud_rain: Integer = $f73d; // cloud-rain
    const cloud_showers_heavy: Integer = $f740; // cloud-showers-heavy
    const cloud_sun: Integer = $f6c4; // cloud-sun
    const cloud_sun_rain: Integer = $f743; // cloud-sun-rain
    const cloud_upload_alt: Integer = $f382; // cloud-upload-alt
    const cloudscale: Integer = $f383; // cloudscale
    const cloudsmith: Integer = $f384; // cloudsmith
    const cloudversify: Integer = $f385; // cloudversify
    const cocktail: Integer = $f561; // cocktail
    const code: Integer = $f121; // code
    const code_branch: Integer = $f126; // code-branch
    const codepen: Integer = $f1cb; // codepen
    const codiepie: Integer = $f284; // codiepie
    const coffee: Integer = $f0f4; // coffee
    const cog: Integer = $f013; // cog
    const cogs: Integer = $f085; // cogs
    const coins: Integer = $f51e; // coins
    const columns: Integer = $f0db; // columns
    const comment: Integer = $f075; // comment
    const comment_alt: Integer = $f27a; // comment-alt
    const comment_dollar: Integer = $f651; // comment-dollar
    const comment_dots: Integer = $f4ad; // comment-dots
    const comment_medical: Integer = $f7f5; // comment-medical
    const comment_slash: Integer = $f4b3; // comment-slash
    const comments: Integer = $f086; // comments
    const comments_dollar: Integer = $f653; // comments-dollar
    const compact_disc: Integer = $f51f; // compact-disc
    const compass: Integer = $f14e; // compass
    const compress: Integer = $f066; // compress
    const compress_alt: Integer = $f422; // compress-alt
    const compress_arrows_alt: Integer = $f78c; // compress-arrows-alt
    const concierge_bell: Integer = $f562; // concierge-bell
    const confluence: Integer = $f78d; // confluence
    const connectdevelop: Integer = $f20e; // connectdevelop
    const contao: Integer = $f26d; // contao
    const cookie: Integer = $f563; // cookie
    const cookie_bite: Integer = $f564; // cookie-bite
    const copy: Integer = $f0c5; // copy
    const copyright: Integer = $f1f9; // copyright
    const cotton_bureau: Integer = $f89e; // cotton-bureau
    const couch: Integer = $f4b8; // couch
    const cpanel: Integer = $f388; // cpanel
    const creative_commons: Integer = $f25e; // creative-commons
    const creative_commons_by: Integer = $f4e7; // creative-commons-by
    const creative_commons_nc: Integer = $f4e8; // creative-commons-nc
    const creative_commons_nc_eu: Integer = $f4e9; // creative-commons-nc-eu
    const creative_commons_nc_jp: Integer = $f4ea; // creative-commons-nc-jp
    const creative_commons_nd: Integer = $f4eb; // creative-commons-nd
    const creative_commons_pd: Integer = $f4ec; // creative-commons-pd
    const creative_commons_pd_alt: Integer = $f4ed; // creative-commons-pd-alt
    const creative_commons_remix: Integer = $f4ee; // creative-commons-remix
    const creative_commons_sa: Integer = $f4ef; // creative-commons-sa
    const creative_commons_sampling: Integer = $f4f0; // creative-commons-sampling
    const creative_commons_sampling_plus: Integer = $f4f1; // creative-commons-sampling-plus
    const creative_commons_share: Integer = $f4f2; // creative-commons-share
    const creative_commons_zero: Integer = $f4f3; // creative-commons-zero
    const credit_card: Integer = $f09d; // credit-card
    const critical_role: Integer = $f6c9; // critical-role
    const crop: Integer = $f125; // crop
    const crop_alt: Integer = $f565; // crop-alt
    const cross: Integer = $f654; // cross
    const crosshairs: Integer = $f05b; // crosshairs
    const crow: Integer = $f520; // crow
    const crown: Integer = $f521; // crown
    const crutch: Integer = $f7f7; // crutch
    const css3: Integer = $f13c; // css3
    const css3_alt: Integer = $f38b; // css3-alt
    const cube: Integer = $f1b2; // cube
    const cubes: Integer = $f1b3; // cubes
    const cut: Integer = $f0c4; // cut
    const cuttlefish: Integer = $f38c; // cuttlefish
    const d_and_d: Integer = $f38d; // d-and-d
    const d_and_d_beyond: Integer = $f6ca; // d-and-d-beyond
    const dailymotion: Integer = $f952; // dailymotion
    const dashcube: Integer = $f210; // dashcube
    const database: Integer = $f1c0; // database
    const deaf: Integer = $f2a4; // deaf
    const delicious: Integer = $f1a5; // delicious
    const democrat: Integer = $f747; // democrat
    const deploydog: Integer = $f38e; // deploydog
    const deskpro: Integer = $f38f; // deskpro
    const desktop: Integer = $f108; // desktop
    const dev: Integer = $f6cc; // dev
    const deviantart: Integer = $f1bd; // deviantart
    const dharmachakra: Integer = $f655; // dharmachakra
    const dhl: Integer = $f790; // dhl
    const diagnoses: Integer = $f470; // diagnoses
    const diaspora: Integer = $f791; // diaspora
    const dice: Integer = $f522; // dice
    const dice_d20: Integer = $f6cf; // dice-d20
    const dice_d6: Integer = $f6d1; // dice-d6
    const dice_five: Integer = $f523; // dice-five
    const dice_four: Integer = $f524; // dice-four
    const dice_one: Integer = $f525; // dice-one
    const dice_six: Integer = $f526; // dice-six
    const dice_three: Integer = $f527; // dice-three
    const dice_two: Integer = $f528; // dice-two
    const digg: Integer = $f1a6; // digg
    const digital_ocean: Integer = $f391; // digital-ocean
    const digital_tachograph: Integer = $f566; // digital-tachograph
    const directions: Integer = $f5eb; // directions
    const discord: Integer = $f392; // discord
    const discourse: Integer = $f393; // discourse
    const disease: Integer = $f7fa; // disease
    const divide: Integer = $f529; // divide
    const dizzy: Integer = $f567; // dizzy
    const dna: Integer = $f471; // dna
    const dochub: Integer = $f394; // dochub
    const docker: Integer = $f395; // docker
    const dog: Integer = $f6d3; // dog
    const dollar_sign: Integer = $f155; // dollar-sign
    const dolly: Integer = $f472; // dolly
    const dolly_flatbed: Integer = $f474; // dolly-flatbed
    const donate: Integer = $f4b9; // donate
    const door_closed: Integer = $f52a; // door-closed
    const door_open: Integer = $f52b; // door-open
    const dot_circle: Integer = $f192; // dot-circle
    const dove: Integer = $f4ba; // dove
    const download: Integer = $f019; // download
    const draft2digital: Integer = $f396; // draft2digital
    const drafting_compass: Integer = $f568; // drafting-compass
    const dragon: Integer = $f6d5; // dragon
    const draw_polygon: Integer = $f5ee; // draw-polygon
    const dribbble: Integer = $f17d; // dribbble
    const dribbble_square: Integer = $f397; // dribbble-square
    const dropbox: Integer = $f16b; // dropbox
    const drum: Integer = $f569; // drum
    const drum_steelpan: Integer = $f56a; // drum-steelpan
    const drumstick_bite: Integer = $f6d7; // drumstick-bite
    const drupal: Integer = $f1a9; // drupal
    const dumbbell: Integer = $f44b; // dumbbell
    const dumpster: Integer = $f793; // dumpster
    const dumpster_fire: Integer = $f794; // dumpster-fire
    const dungeon: Integer = $f6d9; // dungeon
    const dyalog: Integer = $f399; // dyalog
    const earlybirds: Integer = $f39a; // earlybirds
    const ebay: Integer = $f4f4; // ebay
    const edge: Integer = $f282; // edge
    const edit: Integer = $f044; // edit
    const egg: Integer = $f7fb; // egg
    const eject: Integer = $f052; // eject
    const elementor: Integer = $f430; // elementor
    const ellipsis_h: Integer = $f141; // ellipsis-h
    const ellipsis_v: Integer = $f142; // ellipsis-v
    const ello: Integer = $f5f1; // ello
    const ember: Integer = $f423; // ember
    const empire: Integer = $f1d1; // empire
    const envelope: Integer = $f0e0; // envelope
    const envelope_open: Integer = $f2b6; // envelope-open
    const envelope_open_text: Integer = $f658; // envelope-open-text
    const envelope_square: Integer = $f199; // envelope-square
    const envira: Integer = $f299; // envira
    const equals: Integer = $f52c; // equals
    const eraser: Integer = $f12d; // eraser
    const erlang: Integer = $f39d; // erlang
    const ethereum: Integer = $f42e; // ethereum
    const ethernet: Integer = $f796; // ethernet
    const etsy: Integer = $f2d7; // etsy
    const euro_sign: Integer = $f153; // euro-sign
    const evernote: Integer = $f839; // evernote
    const exchange_alt: Integer = $f362; // exchange-alt
    const exclamation: Integer = $f12a; // exclamation
    const exclamation_circle: Integer = $f06a; // exclamation-circle
    const exclamation_triangle: Integer = $f071; // exclamation-triangle
    const expand: Integer = $f065; // expand
    const expand_alt: Integer = $f424; // expand-alt
    const expand_arrows_alt: Integer = $f31e; // expand-arrows-alt
    const expeditedssl: Integer = $f23e; // expeditedssl
    const external_link_alt: Integer = $f35d; // external-link-alt
    const external_link_square_alt: Integer = $f360; // external-link-square-alt
    const eye: Integer = $f06e; // eye
    const eye_dropper: Integer = $f1fb; // eye-dropper
    const eye_slash: Integer = $f070; // eye-slash
    const facebook: Integer = $f09a; // facebook
    const facebook_f: Integer = $f39e; // facebook-f
    const facebook_messenger: Integer = $f39f; // facebook-messenger
    const facebook_square: Integer = $f082; // facebook-square
    const fan: Integer = $f863; // fan
    const fantasy_flight_games: Integer = $f6dc; // fantasy-flight-games
    const fast_backward: Integer = $f049; // fast-backward
    const fast_forward: Integer = $f050; // fast-forward
    const faucet: Integer = $f905; // faucet
    const fax: Integer = $f1ac; // fax
    const feather: Integer = $f52d; // feather
    const feather_alt: Integer = $f56b; // feather-alt
    const fedex: Integer = $f797; // fedex
    const fedora: Integer = $f798; // fedora
    const female: Integer = $f182; // female
    const fighter_jet: Integer = $f0fb; // fighter-jet
    const figma: Integer = $f799; // figma
    const &file: Integer = $f15b; // file
    const file_alt: Integer = $f15c; // file-alt
    const file_archive: Integer = $f1c6; // file-archive
    const file_audio: Integer = $f1c7; // file-audio
    const file_code: Integer = $f1c9; // file-code
    const file_contract: Integer = $f56c; // file-contract
    const file_csv: Integer = $f6dd; // file-csv
    const file_download: Integer = $f56d; // file-download
    const file_excel: Integer = $f1c3; // file-excel
    const file_export: Integer = $f56e; // file-export
    const file_image: Integer = $f1c5; // file-image
    const file_import: Integer = $f56f; // file-import
    const file_invoice: Integer = $f570; // file-invoice
    const file_invoice_dollar: Integer = $f571; // file-invoice-dollar
    const file_medical: Integer = $f477; // file-medical
    const file_medical_alt: Integer = $f478; // file-medical-alt
    const file_pdf: Integer = $f1c1; // file-pdf
    const file_powerpoint: Integer = $f1c4; // file-powerpoint
    const file_prescription: Integer = $f572; // file-prescription
    const file_signature: Integer = $f573; // file-signature
    const file_upload: Integer = $f574; // file-upload
    const file_video: Integer = $f1c8; // file-video
    const file_word: Integer = $f1c2; // file-word
    const fill: Integer = $f575; // fill
    const fill_drip: Integer = $f576; // fill-drip
    const film: Integer = $f008; // film
    const filter: Integer = $f0b0; // filter
    const fingerprint: Integer = $f577; // fingerprint
    const fire: Integer = $f06d; // fire
    const fire_alt: Integer = $f7e4; // fire-alt
    const fire_extinguisher: Integer = $f134; // fire-extinguisher
    const firefox: Integer = $f269; // firefox
    const firefox_browser: Integer = $f907; // firefox-browser
    const first_aid: Integer = $f479; // first-aid
    const first_order: Integer = $f2b0; // first-order
    const first_order_alt: Integer = $f50a; // first-order-alt
    const firstdraft: Integer = $f3a1; // firstdraft
    const fish: Integer = $f578; // fish
    const fist_raised: Integer = $f6de; // fist-raised
    const flag: Integer = $f024; // flag
    const flag_checkered: Integer = $f11e; // flag-checkered
    const flag_usa: Integer = $f74d; // flag-usa
    const flask: Integer = $f0c3; // flask
    const flickr: Integer = $f16e; // flickr
    const flipboard: Integer = $f44d; // flipboard
    const flushed: Integer = $f579; // flushed
    const fly: Integer = $f417; // fly
    const folder: Integer = $f07b; // folder
    const folder_minus: Integer = $f65d; // folder-minus
    const folder_open: Integer = $f07c; // folder-open
    const folder_plus: Integer = $f65e; // folder-plus
    const font: Integer = $f031; // font
    const font_awesome: Integer = $f2b4; // font-awesome
    const font_awesome_alt: Integer = $f35c; // font-awesome-alt
    const font_awesome_flag: Integer = $f425; // font-awesome-flag
    const font_awesome_logo_full: Integer = $f4e6; // font-awesome-logo-full
    const fonticons: Integer = $f280; // fonticons
    const fonticons_fi: Integer = $f3a2; // fonticons-fi
    const football_ball: Integer = $f44e; // football-ball
    const fort_awesome: Integer = $f286; // fort-awesome
    const fort_awesome_alt: Integer = $f3a3; // fort-awesome-alt
    const forumbee: Integer = $f211; // forumbee
    const forward: Integer = $f04e; // forward
    const foursquare: Integer = $f180; // foursquare
    const free_code_camp: Integer = $f2c5; // free-code-camp
    const freebsd: Integer = $f3a4; // freebsd
    const frog: Integer = $f52e; // frog
    const frown: Integer = $f119; // frown
    const frown_open: Integer = $f57a; // frown-open
    const fulcrum: Integer = $f50b; // fulcrum
    const funnel_dollar: Integer = $f662; // funnel-dollar
    const futbol: Integer = $f1e3; // futbol
    const galactic_republic: Integer = $f50c; // galactic-republic
    const galactic_senate: Integer = $f50d; // galactic-senate
    const gamepad: Integer = $f11b; // gamepad
    const gas_pump: Integer = $f52f; // gas-pump
    const gavel: Integer = $f0e3; // gavel
    const gem: Integer = $f3a5; // gem
    const genderless: Integer = $f22d; // genderless
    const get_pocket: Integer = $f265; // get-pocket
    const gg: Integer = $f260; // gg
    const gg_circle: Integer = $f261; // gg-circle
    const ghost: Integer = $f6e2; // ghost
    const gift: Integer = $f06b; // gift
    const gifts: Integer = $f79c; // gifts
    const git: Integer = $f1d3; // git
    const git_alt: Integer = $f841; // git-alt
    const git_square: Integer = $f1d2; // git-square
    const github: Integer = $f09b; // github
    const github_alt: Integer = $f113; // github-alt
    const github_square: Integer = $f092; // github-square
    const gitkraken: Integer = $f3a6; // gitkraken
    const gitlab: Integer = $f296; // gitlab
    const gitter: Integer = $f426; // gitter
    const glass_cheers: Integer = $f79f; // glass-cheers
    const glass_martini: Integer = $f000; // glass-martini
    const glass_martini_alt: Integer = $f57b; // glass-martini-alt
    const glass_whiskey: Integer = $f7a0; // glass-whiskey
    const glasses: Integer = $f530; // glasses
    const glide: Integer = $f2a5; // glide
    const glide_g: Integer = $f2a6; // glide-g
    const globe: Integer = $f0ac; // globe
    const globe_africa: Integer = $f57c; // globe-africa
    const globe_americas: Integer = $f57d; // globe-americas
    const globe_asia: Integer = $f57e; // globe-asia
    const globe_europe: Integer = $f7a2; // globe-europe
    const gofore: Integer = $f3a7; // gofore
    const golf_ball: Integer = $f450; // golf-ball
    const goodreads: Integer = $f3a8; // goodreads
    const goodreads_g: Integer = $f3a9; // goodreads-g
    const google: Integer = $f1a0; // google
    const google_drive: Integer = $f3aa; // google-drive
    const google_play: Integer = $f3ab; // google-play
    const google_plus: Integer = $f2b3; // google-plus
    const google_plus_g: Integer = $f0d5; // google-plus-g
    const google_plus_square: Integer = $f0d4; // google-plus-square
    const google_wallet: Integer = $f1ee; // google-wallet
    const gopuram: Integer = $f664; // gopuram
    const graduation_cap: Integer = $f19d; // graduation-cap
    const gratipay: Integer = $f184; // gratipay
    const grav: Integer = $f2d6; // grav
    const greater_than: Integer = $f531; // greater-than
    const greater_than_equal: Integer = $f532; // greater-than-equal
    const grimace: Integer = $f57f; // grimace
    const grin: Integer = $f580; // grin
    const grin_alt: Integer = $f581; // grin-alt
    const grin_beam: Integer = $f582; // grin-beam
    const grin_beam_sweat: Integer = $f583; // grin-beam-sweat
    const grin_hearts: Integer = $f584; // grin-hearts
    const grin_squint: Integer = $f585; // grin-squint
    const grin_squint_tears: Integer = $f586; // grin-squint-tears
    const grin_stars: Integer = $f587; // grin-stars
    const grin_tears: Integer = $f588; // grin-tears
    const grin_tongue: Integer = $f589; // grin-tongue
    const grin_tongue_squint: Integer = $f58a; // grin-tongue-squint
    const grin_tongue_wink: Integer = $f58b; // grin-tongue-wink
    const grin_wink: Integer = $f58c; // grin-wink
    const grip_horizontal: Integer = $f58d; // grip-horizontal
    const grip_lines: Integer = $f7a4; // grip-lines
    const grip_lines_vertical: Integer = $f7a5; // grip-lines-vertical
    const grip_vertical: Integer = $f58e; // grip-vertical
    const gripfire: Integer = $f3ac; // gripfire
    const grunt: Integer = $f3ad; // grunt
    const guitar: Integer = $f7a6; // guitar
    const gulp: Integer = $f3ae; // gulp
    const h_square: Integer = $f0fd; // h-square
    const hacker_news: Integer = $f1d4; // hacker-news
    const hacker_news_square: Integer = $f3af; // hacker-news-square
    const hackerrank: Integer = $f5f7; // hackerrank
    const hamburger: Integer = $f805; // hamburger
    const hammer: Integer = $f6e3; // hammer
    const hamsa: Integer = $f665; // hamsa
    const hand_holding: Integer = $f4bd; // hand-holding
    const hand_holding_heart: Integer = $f4be; // hand-holding-heart
    const hand_holding_medical: Integer = $f95c; // hand-holding-medical
    const hand_holding_usd: Integer = $f4c0; // hand-holding-usd
    const hand_holding_water: Integer = $f4c1; // hand-holding-water
    const hand_lizard: Integer = $f258; // hand-lizard
    const hand_middle_finger: Integer = $f806; // hand-middle-finger
    const hand_paper: Integer = $f256; // hand-paper
    const hand_peace: Integer = $f25b; // hand-peace
    const hand_point_down: Integer = $f0a7; // hand-point-down
    const hand_point_left: Integer = $f0a5; // hand-point-left
    const hand_point_right: Integer = $f0a4; // hand-point-right
    const hand_point_up: Integer = $f0a6; // hand-point-up
    const hand_pointer: Integer = $f25a; // hand-pointer
    const hand_rock: Integer = $f255; // hand-rock
    const hand_scissors: Integer = $f257; // hand-scissors
    const hand_sparkles: Integer = $f95d; // hand-sparkles
    const hand_spock: Integer = $f259; // hand-spock
    const hands: Integer = $f4c2; // hands
    const hands_helping: Integer = $f4c4; // hands-helping
    const hands_wash: Integer = $f95e; // hands-wash
    const handshake: Integer = $f2b5; // handshake
    const handshake_alt_slash: Integer = $f95f; // handshake-alt-slash
    const handshake_slash: Integer = $f960; // handshake-slash
    const hanukiah: Integer = $f6e6; // hanukiah
    const hard_hat: Integer = $f807; // hard-hat
    const hashtag: Integer = $f292; // hashtag
    const hat_cowboy: Integer = $f8c0; // hat-cowboy
    const hat_cowboy_side: Integer = $f8c1; // hat-cowboy-side
    const hat_wizard: Integer = $f6e8; // hat-wizard
    const hdd: Integer = $f0a0; // hdd
    const head_side_cough: Integer = $f961; // head-side-cough
    const head_side_cough_slash: Integer = $f962; // head-side-cough-slash
    const head_side_mask: Integer = $f963; // head-side-mask
    const head_side_virus: Integer = $f964; // head-side-virus
    const heading: Integer = $f1dc; // heading
    const headphones: Integer = $f025; // headphones
    const headphones_alt: Integer = $f58f; // headphones-alt
    const headset: Integer = $f590; // headset
    const heart: Integer = $f004; // heart
    const heart_broken: Integer = $f7a9; // heart-broken
    const heartbeat: Integer = $f21e; // heartbeat
    const helicopter: Integer = $f533; // helicopter
    const highlighter: Integer = $f591; // highlighter
    const hiking: Integer = $f6ec; // hiking
    const hippo: Integer = $f6ed; // hippo
    const hips: Integer = $f452; // hips
    const hire_a_helper: Integer = $f3b0; // hire-a-helper
    const history: Integer = $f1da; // history
    const hockey_puck: Integer = $f453; // hockey-puck
    const holly_berry: Integer = $f7aa; // holly-berry
    const home: Integer = $f015; // home
    const hooli: Integer = $f427; // hooli
    const hornbill: Integer = $f592; // hornbill
    const horse: Integer = $f6f0; // horse
    const horse_head: Integer = $f7ab; // horse-head
    const hospital: Integer = $f0f8; // hospital
    const hospital_alt: Integer = $f47d; // hospital-alt
    const hospital_symbol: Integer = $f47e; // hospital-symbol
    const hospital_user: Integer = $f80d; // hospital-user
    const hot_tub: Integer = $f593; // hot-tub
    const hotdog: Integer = $f80f; // hotdog
    const hotel: Integer = $f594; // hotel
    const hotjar: Integer = $f3b1; // hotjar
    const hourglass: Integer = $f254; // hourglass
    const hourglass_end: Integer = $f253; // hourglass-end
    const hourglass_half: Integer = $f252; // hourglass-half
    const hourglass_start: Integer = $f251; // hourglass-start
    const house_damage: Integer = $f6f1; // house-damage
    const house_user: Integer = $f965; // house-user
    const houzz: Integer = $f27c; // houzz
    const hryvnia: Integer = $f6f2; // hryvnia
    const html5: Integer = $f13b; // html5
    const hubspot: Integer = $f3b2; // hubspot
    const i_cursor: Integer = $f246; // i-cursor
    const ice_cream: Integer = $f810; // ice-cream
    const icicles: Integer = $f7ad; // icicles
    const icons: Integer = $f86d; // icons
    const id_badge: Integer = $f2c1; // id-badge
    const id_card: Integer = $f2c2; // id-card
    const id_card_alt: Integer = $f47f; // id-card-alt
    const ideal: Integer = $f913; // ideal
    const igloo: Integer = $f7ae; // igloo
    const image: Integer = $f03e; // image
    const images: Integer = $f302; // images
    const imdb: Integer = $f2d8; // imdb
    const inbox: Integer = $f01c; // inbox
    const indent: Integer = $f03c; // indent
    const industry: Integer = $f275; // industry
    const infinity: Integer = $f534; // infinity
    const info: Integer = $f129; // info
    const info_circle: Integer = $f05a; // info-circle
    const instagram: Integer = $f16d; // instagram
    const instagram_square: Integer = $f955; // instagram-square
    const intercom: Integer = $f7af; // intercom
    const internet_explorer: Integer = $f26b; // internet-explorer
    const invision: Integer = $f7b0; // invision
    const ioxhost: Integer = $f208; // ioxhost
    const italic: Integer = $f033; // italic
    const itch_io: Integer = $f83a; // itch-io
    const itunes: Integer = $f3b4; // itunes
    const itunes_note: Integer = $f3b5; // itunes-note
    const java: Integer = $f4e4; // java
    const jedi: Integer = $f669; // jedi
    const jedi_order: Integer = $f50e; // jedi-order
    const jenkins: Integer = $f3b6; // jenkins
    const jira: Integer = $f7b1; // jira
    const joget: Integer = $f3b7; // joget
    const joint: Integer = $f595; // joint
    const joomla: Integer = $f1aa; // joomla
    const journal_whills: Integer = $f66a; // journal-whills
    const js: Integer = $f3b8; // js
    const js_square: Integer = $f3b9; // js-square
    const jsfiddle: Integer = $f1cc; // jsfiddle
    const kaaba: Integer = $f66b; // kaaba
    const kaggle: Integer = $f5fa; // kaggle
    const key: Integer = $f084; // key
    const keybase: Integer = $f4f5; // keybase
    const keyboard: Integer = $f11c; // keyboard
    const keycdn: Integer = $f3ba; // keycdn
    const khanda: Integer = $f66d; // khanda
    const kickstarter: Integer = $f3bb; // kickstarter
    const kickstarter_k: Integer = $f3bc; // kickstarter-k
    const kiss: Integer = $f596; // kiss
    const kiss_beam: Integer = $f597; // kiss-beam
    const kiss_wink_heart: Integer = $f598; // kiss-wink-heart
    const kiwi_bird: Integer = $f535; // kiwi-bird
    const korvue: Integer = $f42f; // korvue
    const landmark: Integer = $f66f; // landmark
    const language: Integer = $f1ab; // language
    const laptop: Integer = $f109; // laptop
    const laptop_code: Integer = $f5fc; // laptop-code
    const laptop_house: Integer = $f966; // laptop-house
    const laptop_medical: Integer = $f812; // laptop-medical
    const laravel: Integer = $f3bd; // laravel
    const lastfm: Integer = $f202; // lastfm
    const lastfm_square: Integer = $f203; // lastfm-square
    const laugh: Integer = $f599; // laugh
    const laugh_beam: Integer = $f59a; // laugh-beam
    const laugh_squint: Integer = $f59b; // laugh-squint
    const laugh_wink: Integer = $f59c; // laugh-wink
    const layer_group: Integer = $f5fd; // layer-group
    const leaf: Integer = $f06c; // leaf
    const leanpub: Integer = $f212; // leanpub
    const lemon: Integer = $f094; // lemon
    const less: Integer = $f41d; // less
    const less_than: Integer = $f536; // less-than
    const less_than_equal: Integer = $f537; // less-than-equal
    const level_down_alt: Integer = $f3be; // level-down-alt
    const level_up_alt: Integer = $f3bf; // level-up-alt
    const life_ring: Integer = $f1cd; // life-ring
    const lightbulb: Integer = $f0eb; // lightbulb
    const line: Integer = $f3c0; // line
    const link: Integer = $f0c1; // link
    const linkedin: Integer = $f08c; // linkedin
    const linkedin_in: Integer = $f0e1; // linkedin-in
    const linode: Integer = $f2b8; // linode
    const linux: Integer = $f17c; // linux
    const lira_sign: Integer = $f195; // lira-sign
    const list: Integer = $f03a; // list
    const list_alt: Integer = $f022; // list-alt
    const list_ol: Integer = $f0cb; // list-ol
    const list_ul: Integer = $f0ca; // list-ul
    const location_arrow: Integer = $f124; // location-arrow
    const lock: Integer = $f023; // lock
    const lock_open: Integer = $f3c1; // lock-open
    const long_arrow_alt_down: Integer = $f309; // long-arrow-alt-down
    const long_arrow_alt_left: Integer = $f30a; // long-arrow-alt-left
    const long_arrow_alt_right: Integer = $f30b; // long-arrow-alt-right
    const long_arrow_alt_up: Integer = $f30c; // long-arrow-alt-up
    const low_vision: Integer = $f2a8; // low-vision
    const luggage_cart: Integer = $f59d; // luggage-cart
    const lungs: Integer = $f604; // lungs
    const lungs_virus: Integer = $f967; // lungs-virus
    const lyft: Integer = $f3c3; // lyft
    const magento: Integer = $f3c4; // magento
    const magic: Integer = $f0d0; // magic
    const magnet: Integer = $f076; // magnet
    const mail_bulk: Integer = $f674; // mail-bulk
    const mailchimp: Integer = $f59e; // mailchimp
    const male: Integer = $f183; // male
    const mandalorian: Integer = $f50f; // mandalorian
    const map: Integer = $f279; // map
    const map_marked: Integer = $f59f; // map-marked
    const map_marked_alt: Integer = $f5a0; // map-marked-alt
    const map_marker: Integer = $f041; // map-marker
    const map_marker_alt: Integer = $f3c5; // map-marker-alt
    const map_pin: Integer = $f276; // map-pin
    const map_signs: Integer = $f277; // map-signs
    const markdown: Integer = $f60f; // markdown
    const marker: Integer = $f5a1; // marker
    const mars: Integer = $f222; // mars
    const mars_double: Integer = $f227; // mars-double
    const mars_stroke: Integer = $f229; // mars-stroke
    const mars_stroke_h: Integer = $f22b; // mars-stroke-h
    const mars_stroke_v: Integer = $f22a; // mars-stroke-v
    const mask: Integer = $f6fa; // mask
    const mastodon: Integer = $f4f6; // mastodon
    const maxcdn: Integer = $f136; // maxcdn
    const mdb: Integer = $f8ca; // mdb
    const medal: Integer = $f5a2; // medal
    const medapps: Integer = $f3c6; // medapps
    const medium: Integer = $f23a; // medium
    const medium_m: Integer = $f3c7; // medium-m
    const medkit: Integer = $f0fa; // medkit
    const medrt: Integer = $f3c8; // medrt
    const meetup: Integer = $f2e0; // meetup
    const megaport: Integer = $f5a3; // megaport
    const meh: Integer = $f11a; // meh
    const meh_blank: Integer = $f5a4; // meh-blank
    const meh_rolling_eyes: Integer = $f5a5; // meh-rolling-eyes
    const memory: Integer = $f538; // memory
    const mendeley: Integer = $f7b3; // mendeley
    const menorah: Integer = $f676; // menorah
    const mercury: Integer = $f223; // mercury
    const meteor: Integer = $f753; // meteor
    const microblog: Integer = $f91a; // microblog
    const microchip: Integer = $f2db; // microchip
    const microphone: Integer = $f130; // microphone
    const microphone_alt: Integer = $f3c9; // microphone-alt
    const microphone_alt_slash: Integer = $f539; // microphone-alt-slash
    const microphone_slash: Integer = $f131; // microphone-slash
    const microscope: Integer = $f610; // microscope
    const microsoft: Integer = $f3ca; // microsoft
    const minus: Integer = $f068; // minus
    const minus_circle: Integer = $f056; // minus-circle
    const minus_square: Integer = $f146; // minus-square
    const mitten: Integer = $f7b5; // mitten
    const mix: Integer = $f3cb; // mix
    const mixcloud: Integer = $f289; // mixcloud
    const mixer: Integer = $f956; // mixer
    const mizuni: Integer = $f3cc; // mizuni
    const mobile: Integer = $f10b; // mobile
    const mobile_alt: Integer = $f3cd; // mobile-alt
    const modx: Integer = $f285; // modx
    const monero: Integer = $f3d0; // monero
    const money_bill: Integer = $f0d6; // money-bill
    const money_bill_alt: Integer = $f3d1; // money-bill-alt
    const money_bill_wave: Integer = $f53a; // money-bill-wave
    const money_bill_wave_alt: Integer = $f53b; // money-bill-wave-alt
    const money_check: Integer = $f53c; // money-check
    const money_check_alt: Integer = $f53d; // money-check-alt
    const monument: Integer = $f5a6; // monument
    const moon: Integer = $f186; // moon
    const mortar_pestle: Integer = $f5a7; // mortar-pestle
    const mosque: Integer = $f678; // mosque
    const motorcycle: Integer = $f21c; // motorcycle
    const mountain: Integer = $f6fc; // mountain
    const mouse: Integer = $f8cc; // mouse
    const mouse_pointer: Integer = $f245; // mouse-pointer
    const mug_hot: Integer = $f7b6; // mug-hot
    const music: Integer = $f001; // music
    const napster: Integer = $f3d2; // napster
    const neos: Integer = $f612; // neos
    const network_wired: Integer = $f6ff; // network-wired
    const neuter: Integer = $f22c; // neuter
    const newspaper: Integer = $f1ea; // newspaper
    const nimblr: Integer = $f5a8; // nimblr
    const node: Integer = $f419; // node
    const node_js: Integer = $f3d3; // node-js
    const not_equal: Integer = $f53e; // not-equal
    const notes_medical: Integer = $f481; // notes-medical
    const npm: Integer = $f3d4; // npm
    const ns8: Integer = $f3d5; // ns8
    const nutritionix: Integer = $f3d6; // nutritionix
    const object_group: Integer = $f247; // object-group
    const object_ungroup: Integer = $f248; // object-ungroup
    const odnoklassniki: Integer = $f263; // odnoklassniki
    const odnoklassniki_square: Integer = $f264; // odnoklassniki-square
    const oil_can: Integer = $f613; // oil-can
    const old_republic: Integer = $f510; // old-republic
    const om: Integer = $f679; // om
    const opencart: Integer = $f23d; // opencart
    const openid: Integer = $f19b; // openid
    const opera: Integer = $f26a; // opera
    const optin_monster: Integer = $f23c; // optin-monster
    const orcid: Integer = $f8d2; // orcid
    const osi: Integer = $f41a; // osi
    const otter: Integer = $f700; // otter
    const outdent: Integer = $f03b; // outdent
    const page4: Integer = $f3d7; // page4
    const pagelines: Integer = $f18c; // pagelines
    const pager: Integer = $f815; // pager
    const paint_brush: Integer = $f1fc; // paint-brush
    const paint_roller: Integer = $f5aa; // paint-roller
    const palette: Integer = $f53f; // palette
    const palfed: Integer = $f3d8; // palfed
    const pallet: Integer = $f482; // pallet
    const paper_plane: Integer = $f1d8; // paper-plane
    const paperclip: Integer = $f0c6; // paperclip
    const parachute_box: Integer = $f4cd; // parachute-box
    const paragraph: Integer = $f1dd; // paragraph
    const parking: Integer = $f540; // parking
    const passport: Integer = $f5ab; // passport
    const pastafarianism: Integer = $f67b; // pastafarianism
    const paste: Integer = $f0ea; // paste
    const patreon: Integer = $f3d9; // patreon
    const pause: Integer = $f04c; // pause
    const pause_circle: Integer = $f28b; // pause-circle
    const paw: Integer = $f1b0; // paw
    const paypal: Integer = $f1ed; // paypal
    const peace: Integer = $f67c; // peace
    const pen: Integer = $f304; // pen
    const pen_alt: Integer = $f305; // pen-alt
    const pen_fancy: Integer = $f5ac; // pen-fancy
    const pen_nib: Integer = $f5ad; // pen-nib
    const pen_square: Integer = $f14b; // pen-square
    const pencil_alt: Integer = $f303; // pencil-alt
    const pencil_ruler: Integer = $f5ae; // pencil-ruler
    const penny_arcade: Integer = $f704; // penny-arcade
    const people_arrows: Integer = $f968; // people-arrows
    const people_carry: Integer = $f4ce; // people-carry
    const pepper_hot: Integer = $f816; // pepper-hot
    const percent: Integer = $f295; // percent
    const percentage: Integer = $f541; // percentage
    const periscope: Integer = $f3da; // periscope
    const person_booth: Integer = $f756; // person-booth
    const phabricator: Integer = $f3db; // phabricator
    const phoenix_framework: Integer = $f3dc; // phoenix-framework
    const phoenix_squadron: Integer = $f511; // phoenix-squadron
    const phone: Integer = $f095; // phone
    const phone_alt: Integer = $f879; // phone-alt
    const phone_slash: Integer = $f3dd; // phone-slash
    const phone_square: Integer = $f098; // phone-square
    const phone_square_alt: Integer = $f87b; // phone-square-alt
    const phone_volume: Integer = $f2a0; // phone-volume
    const photo_video: Integer = $f87c; // photo-video
    const php: Integer = $f457; // php
    const pied_piper: Integer = $f2ae; // pied-piper
    const pied_piper_alt: Integer = $f1a8; // pied-piper-alt
    const pied_piper_hat: Integer = $f4e5; // pied-piper-hat
    const pied_piper_pp: Integer = $f1a7; // pied-piper-pp
    const pied_piper_square: Integer = $f91e; // pied-piper-square
    const piggy_bank: Integer = $f4d3; // piggy-bank
    const pills: Integer = $f484; // pills
    const pinterest: Integer = $f0d2; // pinterest
    const pinterest_p: Integer = $f231; // pinterest-p
    const pinterest_square: Integer = $f0d3; // pinterest-square
    const pizza_slice: Integer = $f818; // pizza-slice
    const place_of_worship: Integer = $f67f; // place-of-worship
    const plane: Integer = $f072; // plane
    const plane_arrival: Integer = $f5af; // plane-arrival
    const plane_departure: Integer = $f5b0; // plane-departure
    const plane_slash: Integer = $f969; // plane-slash
    const play: Integer = $f04b; // play
    const play_circle: Integer = $f144; // play-circle
    const playstation: Integer = $f3df; // playstation
    const plug: Integer = $f1e6; // plug
    const plus: Integer = $f067; // plus
    const plus_circle: Integer = $f055; // plus-circle
    const plus_square: Integer = $f0fe; // plus-square
    const podcast: Integer = $f2ce; // podcast
    const poll: Integer = $f681; // poll
    const poll_h: Integer = $f682; // poll-h
    const poo: Integer = $f2fe; // poo
    const poo_storm: Integer = $f75a; // poo-storm
    const poop: Integer = $f619; // poop
    const portrait: Integer = $f3e0; // portrait
    const pound_sign: Integer = $f154; // pound-sign
    const power_off: Integer = $f011; // power-off
    const pray: Integer = $f683; // pray
    const praying_hands: Integer = $f684; // praying-hands
    const prescription: Integer = $f5b1; // prescription
    const prescription_bottle: Integer = $f485; // prescription-bottle
    const prescription_bottle_alt: Integer = $f486; // prescription-bottle-alt
    const print: Integer = $f02f; // print
    const procedures: Integer = $f487; // procedures
    const product_hunt: Integer = $f288; // product-hunt
    const project_diagram: Integer = $f542; // project-diagram
    const pump_medical: Integer = $f96a; // pump-medical
    const pump_soap: Integer = $f96b; // pump-soap
    const pushed: Integer = $f3e1; // pushed
    const puzzle_piece: Integer = $f12e; // puzzle-piece
    const python: Integer = $f3e2; // python
    const qq: Integer = $f1d6; // qq
    const qrcode: Integer = $f029; // qrcode
    const question: Integer = $f128; // question
    const question_circle: Integer = $f059; // question-circle
    const quidditch: Integer = $f458; // quidditch
    const quinscape: Integer = $f459; // quinscape
    const quora: Integer = $f2c4; // quora
    const quote_left: Integer = $f10d; // quote-left
    const quote_right: Integer = $f10e; // quote-right
    const quran: Integer = $f687; // quran
    const r_project: Integer = $f4f7; // r-project
    const radiation: Integer = $f7b9; // radiation
    const radiation_alt: Integer = $f7ba; // radiation-alt
    const rainbow: Integer = $f75b; // rainbow
    const random: Integer = $f074; // random
    const raspberry_pi: Integer = $f7bb; // raspberry-pi
    const ravelry: Integer = $f2d9; // ravelry
    const react: Integer = $f41b; // react
    const reacteurope: Integer = $f75d; // reacteurope
    const readme: Integer = $f4d5; // readme
    const rebel: Integer = $f1d0; // rebel
    const receipt: Integer = $f543; // receipt
    const record_vinyl: Integer = $f8d9; // record-vinyl
    const recycle: Integer = $f1b8; // recycle
    const red_river: Integer = $f3e3; // red-river
    const reddit: Integer = $f1a1; // reddit
    const reddit_alien: Integer = $f281; // reddit-alien
    const reddit_square: Integer = $f1a2; // reddit-square
    const redhat: Integer = $f7bc; // redhat
    const redo: Integer = $f01e; // redo
    const redo_alt: Integer = $f2f9; // redo-alt
    const registered: Integer = $f25d; // registered
    const remove_format: Integer = $f87d; // remove-format
    const renren: Integer = $f18b; // renren
    const reply: Integer = $f3e5; // reply
    const reply_all: Integer = $f122; // reply-all
    const replyd: Integer = $f3e6; // replyd
    const republican: Integer = $f75e; // republican
    const researchgate: Integer = $f4f8; // researchgate
    const resolving: Integer = $f3e7; // resolving
    const restroom: Integer = $f7bd; // restroom
    const retweet: Integer = $f079; // retweet
    const rev: Integer = $f5b2; // rev
    const ribbon: Integer = $f4d6; // ribbon
    const ring: Integer = $f70b; // ring
    const road: Integer = $f018; // road
    const robot: Integer = $f544; // robot
    const rocket: Integer = $f135; // rocket
    const rocketchat: Integer = $f3e8; // rocketchat
    const rockrms: Integer = $f3e9; // rockrms
    const route: Integer = $f4d7; // route
    const rss: Integer = $f09e; // rss
    const rss_square: Integer = $f143; // rss-square
    const ruble_sign: Integer = $f158; // ruble-sign
    const ruler: Integer = $f545; // ruler
    const ruler_combined: Integer = $f546; // ruler-combined
    const ruler_horizontal: Integer = $f547; // ruler-horizontal
    const ruler_vertical: Integer = $f548; // ruler-vertical
    const running: Integer = $f70c; // running
    const rupee_sign: Integer = $f156; // rupee-sign
    const sad_cry: Integer = $f5b3; // sad-cry
    const sad_tear: Integer = $f5b4; // sad-tear
    const safari: Integer = $f267; // safari
    const salesforce: Integer = $f83b; // salesforce
    const sass: Integer = $f41e; // sass
    const satellite: Integer = $f7bf; // satellite
    const satellite_dish: Integer = $f7c0; // satellite-dish
    const save: Integer = $f0c7; // save
    const schlix: Integer = $f3ea; // schlix
    const school: Integer = $f549; // school
    const screwdriver: Integer = $f54a; // screwdriver
    const scribd: Integer = $f28a; // scribd
    const scroll: Integer = $f70e; // scroll
    const sd_card: Integer = $f7c2; // sd-card
    const search: Integer = $f002; // search
    const search_dollar: Integer = $f688; // search-dollar
    const search_location: Integer = $f689; // search-location
    const search_minus: Integer = $f010; // search-minus
    const search_plus: Integer = $f00e; // search-plus
    const searchengin: Integer = $f3eb; // searchengin
    const seedling: Integer = $f4d8; // seedling
    const sellcast: Integer = $f2da; // sellcast
    const sellsy: Integer = $f213; // sellsy
    const server: Integer = $f233; // server
    const servicestack: Integer = $f3ec; // servicestack
    const shapes: Integer = $f61f; // shapes
    const share: Integer = $f064; // share
    const share_alt: Integer = $f1e0; // share-alt
    const share_alt_square: Integer = $f1e1; // share-alt-square
    const share_square: Integer = $f14d; // share-square
    const shekel_sign: Integer = $f20b; // shekel-sign
    const shield_alt: Integer = $f3ed; // shield-alt
    const shield_virus: Integer = $f96c; // shield-virus
    const ship: Integer = $f21a; // ship
    const shipping_fast: Integer = $f48b; // shipping-fast
    const shirtsinbulk: Integer = $f214; // shirtsinbulk
    const shoe_prints: Integer = $f54b; // shoe-prints
    const shopify: Integer = $f957; // shopify
    const shopping_bag: Integer = $f290; // shopping-bag
    const shopping_basket: Integer = $f291; // shopping-basket
    const shopping_cart: Integer = $f07a; // shopping-cart
    const shopware: Integer = $f5b5; // shopware
    const shower: Integer = $f2cc; // shower
    const shuttle_van: Integer = $f5b6; // shuttle-van
    const sign: Integer = $f4d9; // sign
    const sign_in_alt: Integer = $f2f6; // sign-in-alt
    const sign_language: Integer = $f2a7; // sign-language
    const sign_out_alt: Integer = $f2f5; // sign-out-alt
    const signal: Integer = $f012; // signal
    const signature: Integer = $f5b7; // signature
    const sim_card: Integer = $f7c4; // sim-card
    const simplybuilt: Integer = $f215; // simplybuilt
    const sistrix: Integer = $f3ee; // sistrix
    const sitemap: Integer = $f0e8; // sitemap
    const sith: Integer = $f512; // sith
    const skating: Integer = $f7c5; // skating
    const sketch: Integer = $f7c6; // sketch
    const skiing: Integer = $f7c9; // skiing
    const skiing_nordic: Integer = $f7ca; // skiing-nordic
    const skull: Integer = $f54c; // skull
    const skull_crossbones: Integer = $f714; // skull-crossbones
    const skyatlas: Integer = $f216; // skyatlas
    const skype: Integer = $f17e; // skype
    const slack: Integer = $f198; // slack
    const slack_hash: Integer = $f3ef; // slack-hash
    const slash: Integer = $f715; // slash
    const sleigh: Integer = $f7cc; // sleigh
    const sliders_h: Integer = $f1de; // sliders-h
    const slideshare: Integer = $f1e7; // slideshare
    const smile: Integer = $f118; // smile
    const smile_beam: Integer = $f5b8; // smile-beam
    const smile_wink: Integer = $f4da; // smile-wink
    const smog: Integer = $f75f; // smog
    const smoking: Integer = $f48d; // smoking
    const smoking_ban: Integer = $f54d; // smoking-ban
    const sms: Integer = $f7cd; // sms
    const snapchat: Integer = $f2ab; // snapchat
    const snapchat_ghost: Integer = $f2ac; // snapchat-ghost
    const snapchat_square: Integer = $f2ad; // snapchat-square
    const snowboarding: Integer = $f7ce; // snowboarding
    const snowflake: Integer = $f2dc; // snowflake
    const snowman: Integer = $f7d0; // snowman
    const snowplow: Integer = $f7d2; // snowplow
    const soap: Integer = $f96e; // soap
    const socks: Integer = $f696; // socks
    const solar_panel: Integer = $f5ba; // solar-panel
    const sort: Integer = $f0dc; // sort
    const sort_alpha_down: Integer = $f15d; // sort-alpha-down
    const sort_alpha_down_alt: Integer = $f881; // sort-alpha-down-alt
    const sort_alpha_up: Integer = $f15e; // sort-alpha-up
    const sort_alpha_up_alt: Integer = $f882; // sort-alpha-up-alt
    const sort_amount_down: Integer = $f160; // sort-amount-down
    const sort_amount_down_alt: Integer = $f884; // sort-amount-down-alt
    const sort_amount_up: Integer = $f161; // sort-amount-up
    const sort_amount_up_alt: Integer = $f885; // sort-amount-up-alt
    const sort_down: Integer = $f0dd; // sort-down
    const sort_numeric_down: Integer = $f162; // sort-numeric-down
    const sort_numeric_down_alt: Integer = $f886; // sort-numeric-down-alt
    const sort_numeric_up: Integer = $f163; // sort-numeric-up
    const sort_numeric_up_alt: Integer = $f887; // sort-numeric-up-alt
    const sort_up: Integer = $f0de; // sort-up
    const soundcloud: Integer = $f1be; // soundcloud
    const sourcetree: Integer = $f7d3; // sourcetree
    const spa: Integer = $f5bb; // spa
    const space_shuttle: Integer = $f197; // space-shuttle
    const speakap: Integer = $f3f3; // speakap
    const speaker_deck: Integer = $f83c; // speaker-deck
    const spell_check: Integer = $f891; // spell-check
    const spider: Integer = $f717; // spider
    const spinner: Integer = $f110; // spinner
    const splotch: Integer = $f5bc; // splotch
    const spotify: Integer = $f1bc; // spotify
    const spray_can: Integer = $f5bd; // spray-can
    const square: Integer = $f0c8; // square
    const square_full: Integer = $f45c; // square-full
    const square_root_alt: Integer = $f698; // square-root-alt
    const squarespace: Integer = $f5be; // squarespace
    const stack_exchange: Integer = $f18d; // stack-exchange
    const stack_overflow: Integer = $f16c; // stack-overflow
    const stackpath: Integer = $f842; // stackpath
    const stamp: Integer = $f5bf; // stamp
    const star: Integer = $f005; // star
    const star_and_crescent: Integer = $f699; // star-and-crescent
    const star_half: Integer = $f089; // star-half
    const star_half_alt: Integer = $f5c0; // star-half-alt
    const star_of_david: Integer = $f69a; // star-of-david
    const star_of_life: Integer = $f621; // star-of-life
    const staylinked: Integer = $f3f5; // staylinked
    const steam: Integer = $f1b6; // steam
    const steam_square: Integer = $f1b7; // steam-square
    const steam_symbol: Integer = $f3f6; // steam-symbol
    const step_backward: Integer = $f048; // step-backward
    const step_forward: Integer = $f051; // step-forward
    const stethoscope: Integer = $f0f1; // stethoscope
    const sticker_mule: Integer = $f3f7; // sticker-mule
    const sticky_note: Integer = $f249; // sticky-note
    const stop: Integer = $f04d; // stop
    const stop_circle: Integer = $f28d; // stop-circle
    const stopwatch: Integer = $f2f2; // stopwatch
    const stopwatch_20: Integer = $f96f; // stopwatch-20
    const store: Integer = $f54e; // store
    const store_alt: Integer = $f54f; // store-alt
    const store_alt_slash: Integer = $f970; // store-alt-slash
    const store_slash: Integer = $f971; // store-slash
    const strava: Integer = $f428; // strava
    const stream: Integer = $f550; // stream
    const street_view: Integer = $f21d; // street-view
    const strikethrough: Integer = $f0cc; // strikethrough
    const stripe: Integer = $f429; // stripe
    const stripe_s: Integer = $f42a; // stripe-s
    const stroopwafel: Integer = $f551; // stroopwafel
    const studiovinari: Integer = $f3f8; // studiovinari
    const stumbleupon: Integer = $f1a4; // stumbleupon
    const stumbleupon_circle: Integer = $f1a3; // stumbleupon-circle
    const subscript: Integer = $f12c; // subscript
    const subway: Integer = $f239; // subway
    const suitcase: Integer = $f0f2; // suitcase
    const suitcase_rolling: Integer = $f5c1; // suitcase-rolling
    const sun: Integer = $f185; // sun
    const superpowers: Integer = $f2dd; // superpowers
    const superscript: Integer = $f12b; // superscript
    const supple: Integer = $f3f9; // supple
    const surprise: Integer = $f5c2; // surprise
    const suse: Integer = $f7d6; // suse
    const swatchbook: Integer = $f5c3; // swatchbook
    const swift: Integer = $f8e1; // swift
    const swimmer: Integer = $f5c4; // swimmer
    const swimming_pool: Integer = $f5c5; // swimming-pool
    const symfony: Integer = $f83d; // symfony
    const synagogue: Integer = $f69b; // synagogue
    const sync: Integer = $f021; // sync
    const sync_alt: Integer = $f2f1; // sync-alt
    const syringe: Integer = $f48e; // syringe
    const table: Integer = $f0ce; // table
    const table_tennis: Integer = $f45d; // table-tennis
    const tablet: Integer = $f10a; // tablet
    const tablet_alt: Integer = $f3fa; // tablet-alt
    const tablets: Integer = $f490; // tablets
    const tachometer_alt: Integer = $f3fd; // tachometer-alt
    const tag: Integer = $f02b; // tag
    const tags: Integer = $f02c; // tags
    const tape: Integer = $f4db; // tape
    const tasks: Integer = $f0ae; // tasks
    const taxi: Integer = $f1ba; // taxi
    const teamspeak: Integer = $f4f9; // teamspeak
    const teeth: Integer = $f62e; // teeth
    const teeth_open: Integer = $f62f; // teeth-open
    const telegram: Integer = $f2c6; // telegram
    const telegram_plane: Integer = $f3fe; // telegram-plane
    const temperature_high: Integer = $f769; // temperature-high
    const temperature_low: Integer = $f76b; // temperature-low
    const tencent_weibo: Integer = $f1d5; // tencent-weibo
    const tenge: Integer = $f7d7; // tenge
    const terminal: Integer = $f120; // terminal
    const text_height: Integer = $f034; // text-height
    const text_width: Integer = $f035; // text-width
    const th: Integer = $f00a; // th
    const th_large: Integer = $f009; // th-large
    const th_list: Integer = $f00b; // th-list
    const the_red_yeti: Integer = $f69d; // the-red-yeti
    const theater_masks: Integer = $f630; // theater-masks
    const themeco: Integer = $f5c6; // themeco
    const themeisle: Integer = $f2b2; // themeisle
    const thermometer: Integer = $f491; // thermometer
    const thermometer_empty: Integer = $f2cb; // thermometer-empty
    const thermometer_full: Integer = $f2c7; // thermometer-full
    const thermometer_half: Integer = $f2c9; // thermometer-half
    const thermometer_quarter: Integer = $f2ca; // thermometer-quarter
    const thermometer_three_quarters: Integer = $f2c8; // thermometer-three-quarters
    const think_peaks: Integer = $f731; // think-peaks
    const thumbs_down: Integer = $f165; // thumbs-down
    const thumbs_up: Integer = $f164; // thumbs-up
    const thumbtack: Integer = $f08d; // thumbtack
    const ticket_alt: Integer = $f3ff; // ticket-alt
    const times: Integer = $f00d; // times
    const times_circle: Integer = $f057; // times-circle
    const tint: Integer = $f043; // tint
    const tint_slash: Integer = $f5c7; // tint-slash
    const tired: Integer = $f5c8; // tired
    const toggle_off: Integer = $f204; // toggle-off
    const toggle_on: Integer = $f205; // toggle-on
    const toilet: Integer = $f7d8; // toilet
    const toilet_paper: Integer = $f71e; // toilet-paper
    const toilet_paper_slash: Integer = $f972; // toilet-paper-slash
    const toolbox: Integer = $f552; // toolbox
    const tools: Integer = $f7d9; // tools
    const tooth: Integer = $f5c9; // tooth
    const torah: Integer = $f6a0; // torah
    const torii_gate: Integer = $f6a1; // torii-gate
    const tractor: Integer = $f722; // tractor
    const trade_federation: Integer = $f513; // trade-federation
    const trademark: Integer = $f25c; // trademark
    const traffic_light: Integer = $f637; // traffic-light
    const trailer: Integer = $f941; // trailer
    const train: Integer = $f238; // train
    const tram: Integer = $f7da; // tram
    const transgender: Integer = $f224; // transgender
    const transgender_alt: Integer = $f225; // transgender-alt
    const trash: Integer = $f1f8; // trash
    const trash_alt: Integer = $f2ed; // trash-alt
    const trash_restore: Integer = $f829; // trash-restore
    const trash_restore_alt: Integer = $f82a; // trash-restore-alt
    const tree: Integer = $f1bb; // tree
    const trello: Integer = $f181; // trello
    const tripadvisor: Integer = $f262; // tripadvisor
    const trophy: Integer = $f091; // trophy
    const truck: Integer = $f0d1; // truck
    const truck_loading: Integer = $f4de; // truck-loading
    const truck_monster: Integer = $f63b; // truck-monster
    const truck_moving: Integer = $f4df; // truck-moving
    const truck_pickup: Integer = $f63c; // truck-pickup
    const tshirt: Integer = $f553; // tshirt
    const tty: Integer = $f1e4; // tty
    const tumblr: Integer = $f173; // tumblr
    const tumblr_square: Integer = $f174; // tumblr-square
    const tv: Integer = $f26c; // tv
    const twitch: Integer = $f1e8; // twitch
    const twitter: Integer = $f099; // twitter
    const twitter_square: Integer = $f081; // twitter-square
    const typo3: Integer = $f42b; // typo3
    const uber: Integer = $f402; // uber
    const ubuntu: Integer = $f7df; // ubuntu
    const uikit: Integer = $f403; // uikit
    const umbraco: Integer = $f8e8; // umbraco
    const umbrella: Integer = $f0e9; // umbrella
    const umbrella_beach: Integer = $f5ca; // umbrella-beach
    const underline: Integer = $f0cd; // underline
    const undo: Integer = $f0e2; // undo
    const undo_alt: Integer = $f2ea; // undo-alt
    const uniregistry: Integer = $f404; // uniregistry
    const unity: Integer = $f949; // unity
    const universal_access: Integer = $f29a; // universal-access
    const university: Integer = $f19c; // university
    const unlink: Integer = $f127; // unlink
    const unlock: Integer = $f09c; // unlock
    const unlock_alt: Integer = $f13e; // unlock-alt
    const untappd: Integer = $f405; // untappd
    const upload: Integer = $f093; // upload
    const ups: Integer = $f7e0; // ups
    const usb: Integer = $f287; // usb
    const user: Integer = $f007; // user
    const user_alt: Integer = $f406; // user-alt
    const user_alt_slash: Integer = $f4fa; // user-alt-slash
    const user_astronaut: Integer = $f4fb; // user-astronaut
    const user_check: Integer = $f4fc; // user-check
    const user_circle: Integer = $f2bd; // user-circle
    const user_clock: Integer = $f4fd; // user-clock
    const user_cog: Integer = $f4fe; // user-cog
    const user_edit: Integer = $f4ff; // user-edit
    const user_friends: Integer = $f500; // user-friends
    const user_graduate: Integer = $f501; // user-graduate
    const user_injured: Integer = $f728; // user-injured
    const user_lock: Integer = $f502; // user-lock
    const user_md: Integer = $f0f0; // user-md
    const user_minus: Integer = $f503; // user-minus
    const user_ninja: Integer = $f504; // user-ninja
    const user_nurse: Integer = $f82f; // user-nurse
    const user_plus: Integer = $f234; // user-plus
    const user_secret: Integer = $f21b; // user-secret
    const user_shield: Integer = $f505; // user-shield
    const user_slash: Integer = $f506; // user-slash
    const user_tag: Integer = $f507; // user-tag
    const user_tie: Integer = $f508; // user-tie
    const user_times: Integer = $f235; // user-times
    const users: Integer = $f0c0; // users
    const users_cog: Integer = $f509; // users-cog
    const usps: Integer = $f7e1; // usps
    const ussunnah: Integer = $f407; // ussunnah
    const utensil_spoon: Integer = $f2e5; // utensil-spoon
    const utensils: Integer = $f2e7; // utensils
    const vaadin: Integer = $f408; // vaadin
    const vector_square: Integer = $f5cb; // vector-square
    const venus: Integer = $f221; // venus
    const venus_double: Integer = $f226; // venus-double
    const venus_mars: Integer = $f228; // venus-mars
    const viacoin: Integer = $f237; // viacoin
    const viadeo: Integer = $f2a9; // viadeo
    const viadeo_square: Integer = $f2aa; // viadeo-square
    const vial: Integer = $f492; // vial
    const vials: Integer = $f493; // vials
    const viber: Integer = $f409; // viber
    const video: Integer = $f03d; // video
    const video_slash: Integer = $f4e2; // video-slash
    const vihara: Integer = $f6a7; // vihara
    const vimeo: Integer = $f40a; // vimeo
    const vimeo_square: Integer = $f194; // vimeo-square
    const vimeo_v: Integer = $f27d; // vimeo-v
    const vine: Integer = $f1ca; // vine
    const virus: Integer = $f974; // virus
    const virus_slash: Integer = $f975; // virus-slash
    const viruses: Integer = $f976; // viruses
    const vk: Integer = $f189; // vk
    const vnv: Integer = $f40b; // vnv
    const voicemail: Integer = $f897; // voicemail
    const volleyball_ball: Integer = $f45f; // volleyball-ball
    const volume_down: Integer = $f027; // volume-down
    const volume_mute: Integer = $f6a9; // volume-mute
    const volume_off: Integer = $f026; // volume-off
    const volume_up: Integer = $f028; // volume-up
    const vote_yea: Integer = $f772; // vote-yea
    const vr_cardboard: Integer = $f729; // vr-cardboard
    const vuejs: Integer = $f41f; // vuejs
    const walking: Integer = $f554; // walking
    const wallet: Integer = $f555; // wallet
    const warehouse: Integer = $f494; // warehouse
    const water: Integer = $f773; // water
    const wave_square: Integer = $f83e; // wave-square
    const waze: Integer = $f83f; // waze
    const weebly: Integer = $f5cc; // weebly
    const weibo: Integer = $f18a; // weibo
    const weight: Integer = $f496; // weight
    const weight_hanging: Integer = $f5cd; // weight-hanging
    const weixin: Integer = $f1d7; // weixin
    const whatsapp: Integer = $f232; // whatsapp
    const whatsapp_square: Integer = $f40c; // whatsapp-square
    const wheelchair: Integer = $f193; // wheelchair
    const whmcs: Integer = $f40d; // whmcs
    const wifi: Integer = $f1eb; // wifi
    const wikipedia_w: Integer = $f266; // wikipedia-w
    const wind: Integer = $f72e; // wind
    const window_close: Integer = $f410; // window-close
    const window_maximize: Integer = $f2d0; // window-maximize
    const window_minimize: Integer = $f2d1; // window-minimize
    const window_restore: Integer = $f2d2; // window-restore
    const windows: Integer = $f17a; // windows
    const wine_bottle: Integer = $f72f; // wine-bottle
    const wine_glass: Integer = $f4e3; // wine-glass
    const wine_glass_alt: Integer = $f5ce; // wine-glass-alt
    const wix: Integer = $f5cf; // wix
    const wizards_of_the_coast: Integer = $f730; // wizards-of-the-coast
    const wolf_pack_battalion: Integer = $f514; // wolf-pack-battalion
    const won_sign: Integer = $f159; // won-sign
    const wordpress: Integer = $f19a; // wordpress
    const wordpress_simple: Integer = $f411; // wordpress-simple
    const wpbeginner: Integer = $f297; // wpbeginner
    const wpexplorer: Integer = $f2de; // wpexplorer
    const wpforms: Integer = $f298; // wpforms
    const wpressr: Integer = $f3e4; // wpressr
    const wrench: Integer = $f0ad; // wrench
    const x_ray: Integer = $f497; // x-ray
    const xbox: Integer = $f412; // xbox
    const xing: Integer = $f168; // xing
    const xing_square: Integer = $f169; // xing-square
    const y_combinator: Integer = $f23b; // y-combinator
    const yahoo: Integer = $f19e; // yahoo
    const yammer: Integer = $f840; // yammer
    const yandex: Integer = $f413; // yandex
    const yandex_international: Integer = $f414; // yandex-international
    const yarn: Integer = $f7e3; // yarn
    const yelp: Integer = $f1e9; // yelp
    const yen_sign: Integer = $f157; // yen-sign
    const yin_yang: Integer = $f6ad; // yin-yang
    const yoast: Integer = $f2b1; // yoast
    const youtube: Integer = $f167; // youtube
    const youtube_square: Integer = $f431; // youtube-square
    const zhihu: Integer = $f63f; // zhihu
  end;

implementation

end.

